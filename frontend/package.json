{
  "name": "sui-send-frontend",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://github.com/suisend-app/payment-tipping",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@mysten/sui.js": "^0.60.0",
    "@mysten/wallet-kit": "^0.8.0",
    "react": "^18.2.0"
  }
}
